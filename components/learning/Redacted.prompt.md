Censored text — the brand's signature device. Wipes left-to-right over 900ms on click.

```jsx
Sie heisst <Redacted>Marlene</Redacted> und ich habe ihr <Redacted>nie gesagt, was ich wollte</Redacted>.
```

Never reveal on hover — looking must be a decision. Redact at most three spans per paragraph, or the page stops being readable. Inside a `Card surface="paper"` pass `tone="paper"`.