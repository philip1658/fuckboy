Profile card for swipe and opener simulations. 3:4, 6px radius — the only rounded card in the system.

```jsx
<SwipeCard name="M." age={27} meta="4 km entfernt" tags={['Klettern','Sarkasmus','Kein Small Talk']} verdict="match" />
```

Redact real names to an initial plus period — the dossier never prints a full name. Pass `image` when the host supplies a photo; otherwise the gradient fallback is intentional, not a placeholder to fill with an illustration.