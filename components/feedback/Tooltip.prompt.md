Small hover explainer — fades in over 140ms, no arrow, no delay animation.

```jsx
<Tooltip content="Misst, wie viel Druck deine Erwartung erzeugt."><Icon name="info" size={14} /></Tooltip>
```

Use for score dimensions and file abbreviations. Never put an action inside a tooltip.