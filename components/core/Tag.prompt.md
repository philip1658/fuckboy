Interactive pill token. The one component in the system allowed a full pill radius alongside PillChoice.

```jsx
<Tag onClick={pick} selected>Humor</Tag>
<Tag onRemove={drop}>Analytisch</Tag>
```

Selected inverts to paper-on-ink. Use for trait pickers, chapter filters and profile attributes — not for status (that is `Badge`).