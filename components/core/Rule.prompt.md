Hairline divider — the system's main structural device. Prefer a rule over a nested card.

```jsx
<Rule />
<Rule tone="danger" label="Red-Flag-Analyse" />
```

Labelled rules open sections inside a reader or dossier panel. Weight stays 1 except the 3px cover frame, which is a border, not a Rule.