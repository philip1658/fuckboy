Checklist control — volt fill with an ink check when set.

```jsx
<Checkbox label="Ich habe die Übung gemacht" description="Ehrlich. Niemand kontrolliert das." checked={done} onChange={…} />
```

Row height respects the 44px tap minimum. For a single on/off setting use `Switch`.