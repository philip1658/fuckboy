Reflection field. Used for every written transfer exercise.

```jsx
<Textarea label="Wie reagierst du normalerweise auf Ablehnung?" rows={5} counter={280} value={v} onChange={…} />
```

Pass `counter` to show a tabular character count. `mono` for field-note entries that appear later in the dossier.