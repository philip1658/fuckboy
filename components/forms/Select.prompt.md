Native select in brand chrome — for short closed lists (chapter, intention, timeframe).

```jsx
<Select label="Was suchst du gerade?" options={[{value:'casual',label:'Unverbindlich'},{value:'open',label:'Offen'}]} />
```

Three or fewer options with short labels? Use `Radio` instead — the brand prefers visible choices.